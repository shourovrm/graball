<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- window base only; real theming is Compose M3 -->
    <style name="Theme.Graball" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- share-sheet activity: transparent window, Compose ModalBottomSheet draws its own scrim -->
    <style name="Theme.Graball.Sheet">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
